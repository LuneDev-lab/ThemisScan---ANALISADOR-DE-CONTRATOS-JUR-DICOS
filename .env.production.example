# .env.local (Produção - Modo Backend)
# Copie este arquivo para .env.local para produção

# Usar backend serverless (recomendado para produção)
VITE_USE_BACKEND=true

# A chave VITE_GEMINI_API_KEY NÃO é necessária quando usando backend
# Configure GEMINI_API_KEY no servidor (Vercel/Netlify dashboard)
